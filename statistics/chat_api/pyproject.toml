[project]
name = "chat_api"
version = "0.4"
dependencies = [
    "python-dotenv",
    "requests==2.31.0",
    "pydantic",
    "asyncio",
    "fastapi",
    "uvicorn"
]

[project.optional-dependencies]
dev = ["black", "flake8"]
